<h1>Hash to Hex CLI</h1>

-- How to use the CLI.

- npm install or yarn add.
- npm build / yarn build.
- in CMD run ./build/cli.js hash <URL> --destination <DESTINATION> (optional:) --throttle <THROTTLE>
